	1. 함수 만드는 것 (정의)
	2. 함수 써먹는 법 (호출)
	3. ( 괄호 )앞에 오는게 함수
	4. 함수는 클래스 안에
	5. 함수 안에 함수 X
	6. 함수 만들 때는 (변수 선언). 이 변수 선언(변수 선언)된걸 파라미터parameter라고 부른다
	함수 호출할 때는 (그냥 값)
	7. Int add(int num1, int num2) { 
	 return num1 + num2;
	}
	리턴타입 함수명(파라미터….){
	//실행내용. 연산..
	Return 값 (vlue);
	}
	
	함수호출
	Int num = 함수명(값(value));
