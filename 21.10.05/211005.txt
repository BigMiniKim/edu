	1. 정수를 보면 4바이트, int를 기본으로 잡음. JVM이 32비트라.
	2. 실수형 상수의 e표기법. 폰트크기 때문에. 콘솔에 표현하기 위해서.
	3. 형변환 중요
	 1)자동 형병환
	 //언제 - 연산시 두개의 타입이 다를 경우
	 //컴퓨터는 연산시 반드시  두 개의 형을 일치 시킨다.
	 long(64비트)를 int로하면(32비트) 32비트가 사라짐.
	 long vs Float float로. 범위가 크고, 소수부 보존을 위해.
	 int범위 이상은 소수부 보존을 원칙으로
	 int범위 이하에 대해서는 int로 형변환
	 2)명시적 형변환
	 short n1 = 10;
	 short n2 = 20;
	 int n3 = n1+n2
	 short로 받아야한다 할때
	 short n3 = (short)(n1 + n2);
	 3)int로 계산을 하면 결과도 int로 저장
	4. 연산자 순위 가장 높은 것 ( )
	5. 복합대인 연산자를 사용하면 형 변환이 필요하지 않다.
